[project]
name = "cbam-screen"
version = "0.1.0"
description = "CBAM borrower screening panel. Portfolio tool, synthetic data only."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
