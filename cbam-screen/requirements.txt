# CBAM Borrower Screening Panel
# Python 3.11 or later. Pinned to major versions.

pandas>=2.2,<3
pyyaml>=6.0,<7
pydantic>=2.7,<3
streamlit>=1.40,<2
plotly>=5.22,<7
pytest>=8.2,<9

# Read-only reader for the Commission default-values workbook (.xlsx).
# Used once by tools/extract_default_values.py, not at app runtime.
openpyxl>=3.1,<4
